import React, { useState } from 'react';

const WinningMode = ({number}) => {
    
    return(
        <>
            <p>you won! the number was {number}</p>
            <button > restart!</button>
        </>
    )
}
export default WinningMode;
